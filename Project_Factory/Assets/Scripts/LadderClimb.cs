using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*

public class LadderClimb : PlayerMovment
{
    [SerializeField] float climbSpeed;

    string PlayerMovment;

    void Start()
    {
        string PlayerMovment = new PlayerMovment();
    }

    void Update()
    {

    }

    void OnTriggerStay2D(Collider2D other)
    {
        if (other.tag == "Player" && Input.GetKey(KeyCode.W))
        {
            other.GetComponent<Rigidbody2D>().velocity = new Vector2(PlayerMovment.speed, climbSpeed);
        }

        else if (other.tag == "Player" && Input.GetKey(KeyCode.S))
        {
            other.GetComponent<Rigidbody2D>().velocity = new Vector2(PlayerMovment.speed, -climbSpeed);
        }

        else if (other.tag == "Player")
        {
            other.GetComponent<Rigidbody2D>().velocity = new Vector2(PlayerMovment.speed, 0);
        }
    }
    
}*/
